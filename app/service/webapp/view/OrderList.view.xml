<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="service.controller.OrderList"
    height="100%"
>
    <Page title="{i18n>orders} ({/Orders/$count})">
        <content>
            <List
                growing="true"
                growingThreshold="6"
                items="{
                    path: '/Orders',
                    parameters: {
                        $expand: 'Items'
                    },
                    sorter: {
                        path: 'createdAt',
                        descending: true
                    }
                }"
            >
                <items>
                    <ObjectListItem
                        title="{i18n>order}: #{= ${ID}.substring(0,8) }"
                        type="Active"
                        press="onOrderPress"
                        number="{
                            path: 'Price_amount',
                            type: 'sap.ui.model.type.Float',
                            formatOptions: {
                                minFractionDigits: 2,
                                maxFractionDigits: 2
                            }
                        }"
                        numberUnit="{Price_currency}"
                    >
                        <attributes>
                            <ObjectAttribute
                                title="{i18n>itemsCount}"
                                text="{Items/$count}"
                            />
                            <ObjectAttribute
                                title="{i18n>createdAt}"
                                text="{
                            path: 'createdAt',
                            formatOptions: {
                                style: 'long'
                            }
                        }"
                            />
                        </attributes>
                    </ObjectListItem>
                </items>
            </List>
        </content>
    </Page>
</mvc:View>
